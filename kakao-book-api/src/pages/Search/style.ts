import styled from 'styled-components';
import { flexCenterLayout, flexLayout } from '../../styles';

export const BookWrapper = styled(flexLayout)`
  gap: 20px;
  margin-top: 20px;
`;

export const SelectorWrapper = styled(flexCenterLayout)`
  gap: 16px;
  font-size: 24px;
`;
